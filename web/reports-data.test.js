import test from 'node:test';import assert from 'node:assert/strict';
import {reportCsv,reportQuery,reportSummary} from './reports-data.js';
test('ringkasan laporan memisahkan lunas dan tunggakan serta menghitung scope unik',()=>{const s=reportSummary([{room_id:'r1',tenant_id:'t1',energy_kwh:'1.25',total_cost_rp:'2000',payment_status:'paid'},{room_id:'r1',tenant_id:'t2',energy_kwh:'2.75',total_cost_rp:'4000',payment_status:'unpaid'}]);assert.deepEqual(s,{energy_kwh:4,total_cost_rp:6000,paid_cost_rp:2000,unpaid_cost_rp:4000,room_count:1,tenant_count:2});});
test('query laporan hanya mengirim filter aktif dan akhir tanggal eksklusif',()=>{const q=new URLSearchParams(reportQuery({period_id:'p',from:'2026-09-01',to:'2026-09-30',room_id:'r',payment_status:'paid'}));assert.equal(q.get('period_id'),'p');assert.equal(q.get('room_id'),'r');assert.equal(q.get('payment_status'),'paid');assert.equal(q.get('from'),'2026-08-31T17:00:00.000Z');assert.equal(q.get('to'),'2026-09-30T17:00:00.000Z');});
test('CSV memuat metadata, seluruh kolom, BOM, separator Excel dan mencegah formula injection',()=>{const csv=reportCsv([{period:'Sep 2026',room:'=cmd',tenant:'Tenant A',energy_kwh:'4.000000000',energy_rate_rp_kwh:'1500',total_cost_rp:'6000',billing_status:'Final',payment_status:'Lunas',paid_at:'25/09/2026 10:00'}],{period:'Sep 2026',range:'Semua',room:'Semua',payment:'Lunas'});assert.ok(csv.startsWith('\uFEFFsep=;'));assert.match(csv,/Nominal Rupiah/);assert.match(csv,/"'=cmd"/);assert.match(csv,/"6000"/);});
