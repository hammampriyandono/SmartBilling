{
  "name": "smartbilling",
  "version": "0.1.0",
  "private": true,
  "description": "Lingkungan pengembangan Capstone A05",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node scripts/check.js",
    "init:local": "node scripts/init-local.js",
    "smoke": "node scripts/smoke.js",
    "verify:persistence": "node scripts/smoke.js --verify-persistence",
    "migrate": "node scripts/migrate.js",
    "seed:demo": "node scripts/seed-demo.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "5.2.1",
    "mqtt": "5.15.2",
    "pg": "8.23.0"
  }
}
